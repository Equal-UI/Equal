import { Colors, Positions } from '@/models/enums'

export const ALLOWED_POSITIONS = [
  Positions.TL,
  Positions.TR,
  Positions.BL,
  Positions.BR,
]

export const ALLOWED_COLORS = [
  Colors.PRIMARY,
  Colors.SUCCESS,
  Colors.DANGER,
  Colors.WARNING,
]
